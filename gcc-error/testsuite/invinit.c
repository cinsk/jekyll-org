

struct s {
  int x;
  int y;
} st = 1;

/**
 * Local Variables:
 * compile-arguments: "-Wall -c invinit.c"
 * description: "struct이나 union으로 선언한 오브젝트에 초기값을 줄 때, \
 *   (중괄호로 싸지 않는 등) 잘못된 초기값을 썼을 때, 이 에러가 발생합니다."
 * End:
 */
