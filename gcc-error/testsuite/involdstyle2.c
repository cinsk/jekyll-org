
int foo(i);

/**
 * Local Variables:
 * compile-arguments: "-Wall -c involdstyle2.c"
 * description: "함수를 선언할 때, ANSI 표준(현재 ISO 표준) 방식과 ANSI 이전 \
 *   방식 중 하나를 쓸 수 있습니다. 전자의 경우, 함수의 리턴 타입과, 파라메터 \
 *   (이름과) 타입을 기록하는 것이고, 후자의 경우, 파라메터 리스트를 쓰지 \
 *   않습니다.  즉, 후자를 쓸 경우, 함수 이름 뒤에 &quot;()&quot;만 써 \
 *   줍니다.  예제에서는 함수 foo()의 첫번째 파라메터의 타입을 쓰지 않았기 \
 *   때문에 이 에러가 발생했습니다."
 * End:
 */
