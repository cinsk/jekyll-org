/* $Id$ */

#include <where_is_it.h>

/**
 * Local Variables:
 * compile-arguments: "-Wall -c noinclude.c"
 * description: "#include에서 포함시킨 헤더 파일을 찾을 수 없을 때, 이 에러가 \
 *   발생합니다.  대개 이 에러는 사용자가 만든 파일을 &quot; 대신 &lt;&gt;로 둘러 싸거나, \
 *   파일 이름을 틀리게 입력하거나, 또는 컴파일러 옵션에서 디렉토리 설정이 잘못되어 있는 경우, \
 *   또는 컴파일러에서 쓰는 환경 변수가 잘 못 지정되어 있는 경우에 발생합니다. 자세한 것은, \
 *   <a href=\"http://gcc.gnu.org/onlinedocs/cpp/Header-Files.html\">Header \
 *   Files</a>를 참고하기 바랍니다."
 * End:
 */
